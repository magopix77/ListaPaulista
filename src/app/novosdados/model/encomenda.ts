import { Time } from "@angular/common";

export class EncomendaModel{
  quantidade: number;
  item: string;
  destino: string;
  sala: number;
  remete: string;
  data: Date;
  hora: Time;
  recebe: string;
  quadra: string;
}
